#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{

    /**
    if (argc < 2)
    {
        printf("Usage: %s <config file>\n", argv[0]);
        return 1;
    }

    if (daemon(0, 0) == -1)
    {
        perror("daemon");
        return 1;
    }

    if (difw_init(argv[1]) == -1)
    {
        return 1;
    }

    if (difw_run() == -1)
    {
        return 1;
    }
    */

    return 0;
}